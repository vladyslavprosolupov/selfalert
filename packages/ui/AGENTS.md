# AGENTS.md

This package owns reusable UI primitives and shared styles.

- Keep it presentational and reusable across apps.
- Do not move business logic, auth state, or API calls into this package.
- Prefer composable components with lightweight props over app-specific wrappers.
