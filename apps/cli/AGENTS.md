# AGENTS.md

This workspace is intentionally a placeholder in the current phase.

- Keep future CLI logic on top of `@selfalert/sdk`.
- Do not move API or dashboard behavior into this workspace yet.
- Keep scripts lightweight until the CLI scope is implemented.
